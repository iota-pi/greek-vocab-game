import {
  Box,
  Stack,
} from '@mui/material';
import { useAppDispatch } from '../../store';


function DataPage() {
  const dispatch = useAppDispatch();

  return (
    <Box padding={2}>
      <Stack spacing={2}>
      </Stack>
    </Box>
  );
}

export default DataPage;
